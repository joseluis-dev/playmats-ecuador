---
import { Chatbot } from '@/components/Chatbot';
import Bento from '@/components/Landing/Bento.astro';
import Prefooter from '@/components/Landing/Prefooter.astro';
import Social from '@/components/Landing/Social.astro';
import Video from '@/components/Landing/Video.astro';
import Layout from '@/layouts/Layout.astro';

export const prerender = false;

const user = await Astro.locals.currentUser()
console.log(user?.emailAddresses.map(email => email.emailAddress))
---

<Layout title="Playmats EC">
	<Video />
	<Bento />
	<Social />
	<Prefooter />
</Layout>
